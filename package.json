{
  "name": "superoffer-frontend-deployment",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix frontend --include=dev && npm run build --prefix frontend -- --configuration production",
    "start": "sh ./start-frontend.sh"
  },
  "dependencies": {
    "node": "24.15.0",
    "serve": "^14.2.4"
  }
}
